<UserControl x:Class="ClassroomManagementApp1.Component.Classroom.ClassroomListViewAssignments"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:ClassroomManagementApp1.Component.Classroom"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <ListView ItemsSource="{Binding AssignmentsFormattedList}"
              ScrollViewer.VerticalScrollBarVisibility="Auto"
              ScrollViewer.HorizontalScrollBarVisibility="Auto">
        
        <ListView.ItemTemplate>
            <DataTemplate>
                <Grid>
                    <StackPanel Margin="5,15,0,0">
                        <TextBlock Text="{Binding description}" FontSize="18" FontFamily="Montserrat Bold" FontWeight="Bold"  Foreground="#b897c6" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding Date}" FontSize="16" FontFamily="Montserrat" Foreground="#b897c6" Margin="0,15,0,0"/>
                    </StackPanel>
                </Grid>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>
